import React from 'react'

export default function App() {
  return (
    <main className="app-shell">
      <section className="starter-card">
        <p className="eyebrow">React starter</p>
        <h1>Medical Viewer</h1>
        <p>
          Your app is ready. Add DICOM loading and rendering when you are ready
          to build the viewer.
        </p>
      </section>
    </main>
  )
}
