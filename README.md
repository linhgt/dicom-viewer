# Medical Viewer

A minimal React + Vite starter for a medical image viewer.

## Run locally

```bash
pnpm install
pnpm dev
```

The starter intentionally contains no DICOM loading, parsing, or viewport setup.
